#!/bin/sh

# https://raw.githubusercontent.com/lucabrunox/try-without-install/refs/heads/main/gcloud

exec docker run --rm -it -v $(pwd):$(pwd) -e HOME -u $(id -u) -v ~/.config/gcloud/:$HOME/.config/gcloud/ google/cloud-sdk gcloud "$@"
