#!/bin/bash

# https://raw.githubusercontent.com/lucabrunox/try-without-install/refs/heads/main/aws

exec docker run --rm -i -v "$PWD:$PWD" -w "$PWD" --network host --env-file <(env | grep -v PATH) -u $(id -u) -v $HOME:$HOME amazon/aws-cli "$@"
