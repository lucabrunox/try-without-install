#!/bin/bash

# https://raw.githubusercontent.com/lucabrunox/try-without-install/refs/heads/main/stern

exec docker run --rm -it -v "$PWD:$PWD" -w "$PWD" --network host --env-file <(env | grep -v PATH) -u $(id -u) -v $HOME:$HOME ghcr.io/stern/stern "$@"